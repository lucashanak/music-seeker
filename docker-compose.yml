services:
  music-seeker:
    build: .
    container_name: music-seeker
    restart: unless-stopped
    ports:
      - "8090:8090"
    env_file:
      - .env
    volumes:
      - ${HOST_MUSIC_DIR:-./music}:/music
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/app/data
