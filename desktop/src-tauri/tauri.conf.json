{
  "$schema": "https://raw.githubusercontent.com/tauri-apps/tauri/dev/crates/tauri-config-schema/schema.json",
  "productName": "MusicSeeker",
  "version": "1.0.0",
  "identifier": "org.hanaktech.musicseeker",
  "build": {
    "frontendDist": "../dist"
  },
  "app": {
    "//": "No windows here on purpose: the window URL is the configured server, which is runtime data. lib.rs builds it (see open_main_window) so the webview is CREATED at that URL rather than navigated to it — capability matching for the initial URL is the documented case, navigation is not.",
    "windows": [],
    "security": {
      "csp": "default-src * 'unsafe-inline' 'unsafe-eval' blob: data: media:;"
    }
  },
  "bundle": {
    "active": true,
    "targets": ["dmg", "app"],
    "icon": [
      "icons/icon.png",
      "icons/icon.icns"
    ],
    "macOS": {
      "minimumSystemVersion": "10.15"
    },
    "android": {
      "minSdkVersion": 24
    }
  }
}
